<%# Form for creating list %>

<%= simple_form_for [@list] do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
  <%= f.input :photo, as::file %>
<% end %>

 <%= link_to 'Nevermind back to top', lists_path %>
